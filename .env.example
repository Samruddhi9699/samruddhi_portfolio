# Optional. The contact form has a working Web3Forms key baked in, so this
# is only needed if you want to rotate the key without editing the code.
# Get a key at https://web3forms.com — it is safe to expose publicly.
# If you set this, set it in your Vercel project settings too.
VITE_WEB3FORMS_KEY=
